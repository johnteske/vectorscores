import drone from "../drone";

const main = d3.select(".main");
const wrapper = main.append("g");

drone(wrapper);

function wail() {
  // long, periodic
  // accented, falling off
}

function alert() {
  // shorter, periodic
}
