#!/bin/bash
#
# Build Jekyll site, lint Ruby files, lint source and built Javascript files

BUNDLE_GEMFILE=bin/Gemfile rake -f bin/Rakefile
ruby bin/eslintfiles.rb
bin/eslint

# Clean up temporary files
rm -rf .tmp
rm .tmp-eslintignore
